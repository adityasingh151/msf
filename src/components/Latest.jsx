import React from 'react';
import NotificationList from './NotificationList'; // Adjust the path as needed

const Latest = () => {
  return (
    <div className="App">
      <NotificationList />
    </div>
  );
};

export default Latest;
